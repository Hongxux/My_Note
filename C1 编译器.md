C1 的优化侧重于局部和快速见效
- **方法内联**：将短小的目标方法代码“复制”到调用方法中，消除方法调用的开销。这是许多优化得以进行的基础。
- **去虚拟化**：如果发现一个虚方法在运行时通常只有一个具体实现，会绕过虚拟调用机制，直接调用目标方法，甚至进行内联。
- **常量传播**：如果识别到一个变量是常量，会将所有使用该变量的地方直接替换为常量值。
- **冗余消除**：移除计算过程中重复或无意义的操作。