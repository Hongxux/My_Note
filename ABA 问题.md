假设线程 1 从内存位置 `X`读取到值 `A`。此时，线程 1 被挂起。
线程 2 运行，将 `X`的值从 `A`改为 `B`，随后又改回 `A`。
然后线程 1 恢复运行，它执行 CAS 操作，发现 `X`的值仍然是 `A`，于是操作成功。

但事实上，这个 `A`已经不再是线程 1 之前读到的那个 `A`了，中间状态 `B`可能已经对程序逻辑产生了影响。


**在一些对状态序列敏感的场景下，例如版本控制、状态机等，忽略中间的状态变化可能会导致数据不一致或逻辑错误。**