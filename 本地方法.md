- 定义：用Java声明，但实现是由C、C++等非Java语言提供的方法，通常由 `native`关键字修饰
- 用途：
	- 与JVM的外部环境交互：Java应用有时需要与Java之外的环境交互，例如直接与操作系统或特定硬件交换信息
	- 避免重复开发：重用那些用C/C++等语言编写的、成熟且高效的库
	- 提高性能： 使用底层语言编写的本地方法可能更高效
- 能力：一旦进入本地方法执行，JVM对其具体行为的管理能力就大大减弱了
	- 通过本地方法接口访问JVM内部的运行时数据区。
	- 直接使用本地处理器中的寄存器。
	- 直接从本地内存堆中分配任意数量的内存。