---
aliases:
  - 一致性
  - 不一致
---
**缓存不一致**：指的是数据库和缓存出现了差异
	在一定时间窗口内（例如删除缓存），缓存中存储的数据副本与底层数据库（或任何被认为是“真实数据源”的系统）中对应的数据出现了**差异**。

这通常发生在数据被修改后。理想情况是修改完成后，所有地方的数据立刻同步。但由于网络延迟、操作失败、并发执行等问题，可能导致缓存中的数据（旧值）未能及时更新或清除，而数据库已经更新为新值。此时，应用程序读取缓存得到的就是过时、错误的数据。