---
aliases:
  - Write-Through
---
**Write-Through 的核心思想是：当应用更新数据时，必须同时更新缓存和数据库，并且这两个操作必须在同一个事务或逻辑单元内完成，都成功后才视为写入成功。**
- 优点：
	- 强一致性
	- 高命中率的读取
		- 更新完数据库不删除缓存而是更新缓存
- 缺点：
	- 写性能差
	- 资源浪费：如果写入的数据后续很少被读取（写多读少），那么频繁更新缓存就是一种资源浪费，因为可能还没来得及被读，就又被新的写操作覆盖了。