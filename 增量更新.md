- 使用时机：原本不可达现在变可达
	- CMS并发标记阶段， 黑色对象（已扫描）新增了对白色对象（未扫描/垃圾）的引用
- 设计思想：只要增加了引用，我就重新看你一眼，防止你漏掉新引用的对象
- 策略：
	- CMS会记录这个**新增**的动作
	- 在重新标记阶段（Remark），CMS会将这些新增引用的**黑色对象**重新变回灰色，然后重新扫描它的引用链，确保新指向的白色对象被标记为存活
- 问题：
	- 需要深度遍历新增引用的根，导致最终阶段STW时间更长
- 好处：
	- 浮动垃圾较少