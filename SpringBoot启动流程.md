 
- Spring Boot 启动流程
	 ```java
		    new SpringApplication(primarySources).run(args) // 启动运行
		```
	1.  实例化 `SpringApplication` 对象：[[实例化SpringApplication前的准备]]
	2. 执行 `run` 方法[[SpringBoot的run方法]]


---
