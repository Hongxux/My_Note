- 需求背景：
	- 旧有的模式，解决并发问题的时候，通常采用严格的锁（旧模式）
		- 这样会阻塞其他事务的读和写（当前读）。在高并发读场景下，这会导致性能急剧下降。
- 解决措施：MVCC+行级锁
	- ​MVCC（解决读写冲突）：创建数据的多个版本（快照）
		- 读操作：无锁读。可以去读一个过期的、但保持一致性的历史版本
		- 写操作：可以继续修改最新版本
	- 行级锁（解决写写冲突）​
- MVCC实现机制：
	- 读操作：通过 **​[[undo log的版本链]] + [[快照|readview]] 可见性规则**实现了 **[[数据库中数据读取机制|快照读]]** 

