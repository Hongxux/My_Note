---
aliases:
  - 初始化块
  - initialization block
---
**初始化块的类型**​：分为对象初始化块（在每次对象构造时执行）和静态初始化块（在类加载时执行，使用`static`关键字）。
**对象初始化块**​：用于在多个构造器间共享初始化代码，例如在Employee类中，对象初始化块设置id字段并递增nextId，确保所有构造器都执行此逻辑。
**静态初始化块**​：用于复杂静态字段初始化，如使用Random类生成随机初始值。静态初始化块在类加载时执行一次。