---
aliases:
  - 构造器
---
- **默认构造器的条件**​：只有在类没有定义任何构造器时，才会自动提供无参数构造器。
	- **手动定义无参数构造器**​：如果类定义了其他构造器（如带参数构造器），则不会自动提供无参数构造器。此时，如果需要无参数构造器，必须显式定义。
- ​**字段初始化**​：无参数构造器（无论是自动还是手动）负责设置字段的初始状态。自动构造器使用默认值，而手动构造器可以设置更有意义的默认值。