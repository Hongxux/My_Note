逃逸分析是 JVM 一种强大的**代码分析技术**（注意，是分析手段，不是直接的优化）。它负责分析一个**新创建的对象的作用域或"动态范围"**，即判断这个对象的引用是否会"逃逸"到方法或线程之外 。
**对象逃逸的三种状态：**
1. **不逃逸**：对象仅在创建它的方法内部被使用，没有传递到外部。这是**优化潜力最大**的情况。
2. **方法逃逸**：对象被作为参数传递给其他方法，或者作为返回值返回。但其他线程无法访问。
3. **线程逃逸**：对象被赋值给可以被其他线程访问的实例变量或静态变量（如 `static`字段）。
    