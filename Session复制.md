- 工作模式：每个服务器上都保存全量的Session数据，并通过网络广播同步变更
- 好处：
	- **高读取性能**：Session数据存储在本地，读取速度快。  
	-  **天然容错**：任何一台服务器都拥有完整数据，一台宕机后用户请求可被其他服务器处理
- 缺点：
	-  **网络带宽消耗大**：数据同步会占用大量内网带宽，可能引发网络风暴。  
	- **内存消耗高**：每台服务器都存储全量Session，内存占用随集群规模和用户量线性增长