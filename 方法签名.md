- 内容：
	- **方法名**
	- **参数类型**

- 返回类型不是签名的一部分。
	- 返回类型不影响重载解析
	- 但覆盖时需保持兼容。
		- **覆盖（override）方法的要求**​：
			-  保持方法签名相同，
			- 子类方法必须至少与超类方法一样可见（如超类public方法，子类不能改为protected）。
			- 返回类型可以协变（covariant），即子类方法可以返回更具体的类型。