- 写屏障的效果：
	- 确保写屏障之前的所有写操作不会被重排序到写屏障之后。
	- 确保写屏障之前的所有写操作结果对其它CPU核心立即可见（写到主内存）。
		- **刷新写缓冲区**：强制当前CPU将写缓冲区中的数据立即写回主内存。
		- **失效其他缓存**：使其他CPU中包含该数据的缓存行失效，强制它们从主内存重新加载。
- 问题：
	- 问题根源： `volatile`的写操作会导致其他CPU的缓存行失效并强制从主内存重新加载
	- 引发的问题：在高并发场景下，如果某个 `volatile`变量被频繁修改，会导致总线流量激增，反而可能成为性能瓶颈。