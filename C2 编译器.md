C2 会进行更多全局和深度的优化，其中很多基于**逃逸分析**​
- **标量替换**：如果分析发现一个对象没有“逃逸”出方法，C2 就不会在堆上创建这个完整的对象，而是将其字段拆解成多个局部变量（标量）来使用。这能显著减少堆内存分配和GC压力。
- **栈上分配**：对于未逃逸的对象，可以直接在栈帧上分配内存，方法执行完毕自动回收，无需垃圾回收器介入。
- **锁消除**：如果发现一个同步锁对象不可能被其他线程访问（即不会发生竞争），则会移除这个同步操作。
- **循环优化**：包括循环展开、循环分离等，提升循环体执行效率。