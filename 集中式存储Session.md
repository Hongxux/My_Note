- 工作模式：将Session数据存储在独立的缓存（如Redis）中，所有应用服务器共享访问
- 好处：
	- 高可用：应用服务器无状态，单点故障不影响Session
		- 成熟的缓存集群（如Redis Cluster）本身具备高可用能力，可以很好地规避单点风险
	- 扩展性好：易于水平扩展应用服务器
	- 数据一致性强：所有服务器读取到的都是同一份Session
- 缺点：每次Session读写都是一次网络操作，存在微小的延迟